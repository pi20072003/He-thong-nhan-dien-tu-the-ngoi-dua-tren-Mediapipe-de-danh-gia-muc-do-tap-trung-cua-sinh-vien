"""
================= LƯU Ý =================

1. Phiên bản thư viện (cực quan trọng):
      TensorFlow: 2.15.0
      Mediapipe: 0.10.21
      Protobuf: 4.25.3
      Scikit-learn: 1.7.1
      OpenCV: 4.11.0
      Pandas: 2.3.1
      NumPy: 1.26.4
      Joblib: 1.5.1

2. Khuyến nghị môi trường:
   - Tạo một virtualenv riêng cho dự án:
        python -m venv .venv
        .venv\Scripts\activate
   - Cài phiên bản phù hợp:
        pip install numpy opencv-python tensorflow scikit-learn PyQt5 mediapipe==0.10.21

3. Yêu cầu về số mẫu:
   - Do có 10 tư thế nên khi chia test_size = 20% cần có ít nhất 30-50 ảnh cho mỗi tư thế
   - Hoặc bỏ stratify=y hoặc giảm test_size = 10% để train ít mẫu hơn

4. Dùng github sau mỗi lần chỉnh sửa xong
   - git add .
   - git commit -m "Mô tả thay đổi"
   - git pull origin main --rebase  # (nếu có khả năng repo trên GitHub đã có commit mới)
   - git push

"""
